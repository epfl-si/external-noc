groups:
  - name: CanaryCanarded
    rules:
    - alert: CanaryCanarded
      expr: |
        up{slug="canari"} == 0
      for: 1m
      labels:
        severity: page
      annotations:
        summary: Math has gone haywire on us
        description: My Description
